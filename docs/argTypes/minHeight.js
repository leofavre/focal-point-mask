import description from './minHeight.md';

const minHeight = {
  name: 'minHeight',
  description,
  table: {
    category: 'Properties',
    type: { summary: 'number' }
  }
};

export default minHeight;
