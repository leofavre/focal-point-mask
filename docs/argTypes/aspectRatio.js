import description from './aspectRatio.md';

const aspectRatio = {
  name: 'aspectRatio',
  description,
  table: {
    category: 'Properties',
    type: { summary: 'string' }
  }
};

export default aspectRatio;
