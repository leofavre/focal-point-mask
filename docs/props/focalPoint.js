import description from './focalPoint.md';

const focalPoint = {
  name: 'focalPoint',
  description,
  table: {
    category: 'Properties',
    type: { summary: 'string' },
    defaultValue: { summary: '"center"' }
  }
};

export default focalPoint;
