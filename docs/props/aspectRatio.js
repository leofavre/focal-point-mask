import description from './aspectRRatio.md';

const aspectRatio = {
  name: 'aspectRatio',
  description,
  table: {
    category: 'Properties',
    type: { summary: 'string' }
  }
};

export default aspectRatio;
